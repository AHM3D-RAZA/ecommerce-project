Options -Indexes

# Block direct browser access to config, core and admin assets source
<IfModule mod_rewrite.c>
    RewriteEngine On
</IfModule>

# Keep secrets out of the browser
<FilesMatch "^\.env">
    Require all denied
</FilesMatch>
